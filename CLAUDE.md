# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

This is a ROS 2 / Gazebo Harmonic simulation package for the **Tulpar IKA** — a 4-wheeled differential-drive ground robot (64 kg, 1200×450×245 mm chassis, Ø400 mm wheels). The package name is `tulpar_description`.

## Build & Run

The colcon workspace root is `~/tulpar_ws`. The repo lives at `~/tulpar_ws/src/tulpar_ika_sim`.

**One-time setup after a fresh clone**: `tulpar_bt/`, `tulpar_ika_msgs/` and `tulpar_obstacle_injector/` all live inside the `tulpar_ika_sim` repo (so they're tracked in the same git history/PR flow), but colcon's recursive package discovery stops descending once it finds `tulpar_ika_sim` itself as a package (repo root = `tulpar_description`). It won't see a package nested inside another package's directory. A symlink at the `src/` level works around this, one per nested package:
```bash
ln -s ~/tulpar_ws/src/tulpar_ika_sim/tulpar_bt ~/tulpar_ws/src/tulpar_bt
ln -s ~/tulpar_ws/src/tulpar_ika_sim/tulpar_ika_msgs ~/tulpar_ws/src/tulpar_ika_msgs
ln -s ~/tulpar_ws/src/tulpar_ika_sim/tulpar_obstacle_injector ~/tulpar_ws/src/tulpar_obstacle_injector
```
These symlinks are workspace-local (not part of the git repo, not committed) — recreate them after every fresh clone/workspace setup. Without them, `colcon build` silently skips these packages.

```bash
# Build
cd ~/tulpar_ws
colcon build --packages-select tulpar_description
colcon build --packages-select tulpar_bt

# Source the workspace (required before ros2 commands)
source ~/tulpar_ws/install/setup.bash

# Launch Gazebo with the default world (S-01 terrain course)
ros2 launch tulpar_description gazebo.launch.py

# Launch with a specific world
ros2 launch tulpar_description gazebo.launch.py world:=/path/to/world.sdf

# Send velocity commands manually (while simulation runs)
ros2 topic pub /cmd_vel geometry_msgs/msg/Twist "{linear: {x: 0.3}}" --once

# Run a test script (Gazebo must be running first)
python3 test_kanit/S01/s01_rampa_testi.py
python3 test_kanit/E02/hizlanma_testi.py
python3 test_kanit/S02/s02_hedef_testi.py
```

## Architecture

### Robot model (`urdf/robot.urdf.xacro`) — Zehra'nın `feature/zehra-faz1-gun1` branch'i temel alındı (16 Temmuz 2026)
**Strateji değişikliği**: Bizim kendi ürettiğimiz decimation tabanlı mesh pipeline'ımız (`govde_v2/v3.stl`, `teker_v2/v3.stl` — global ve parça-bazlı decimation denemeleri) terk edildi ve o dosyalar silindi. Mekanik ekip orijinal `tulpar_ika_assembly.stl`'i Blender'da inceleyip tam/eksiksiz olduğunu doğruladı; bizim decimation'ımız küçük-orta braketleri kaybediyordu (kök neden bulundu: global quadric decimation küçük parçaları düşük-öncelikli sanıp siliyor, parça-bazlı decimation da tam çözemedi). Bunun yerine Zehra'nın `~/tulpar_ika_sim_incele` (feature/zehra-faz1-gun1 checkout) içindeki `urdf/robot.urdf.xacro` + ham `tulpar_ika_assembly.stl` (149MB, basitleştirilmemiş) bizim dosyalarımızın YERİNE kondu; sadece fiziksel parametreler (boyut/kütle/dingil/iz/tekerlek yarıçapı/inertia) mekanik ekipten gelen güncel değerlerle değiştirildi.
- Frame hierarchy: `base_footprint` → `base_link` → wheels (FL/RL/FR/RR), `lidar_link`(+`laser_frame`), `d435if_link`(+`d435if_color_frame`/`d435if_color_optical_frame`/`d435if_depth_frame`/`d435if_depth_optical_frame`, REP-103 optik frame hiyerarşisi), `rpi_hq_camera_link`(+optical frame), `rear_camera_link`(+optical frame)
- **Sensör link isimleri Zehra'nın adlandırmasıyla birleşti**: `d435i_link` (bizim eski `d435if_link` yerine) ve `rear_camera_link` (bizim eski `sjcam_link` yerine) — Zehra'nınkiler zaten gerçek Gazebo sensor plugin'ine bağlıydı, bizimkiler sadece placeholder'dı. `lidar_link`/`rpi_hq_camera_link` isim olarak zaten aynıydı.
- `base_link` sits 0.200 m above ground (yeni tekerlek yarıçapı); `base_footprint` is at ground level
- Şasi: 1200×450×245mm, 64kg (mekanik ekipten) — collision box + kütle + inertia güncellendi: `ixx=1.4001 iyy=8.0001 izz=8.7600` (analitik kutu, üçgen eşitsizliği doğrulandı; collision origin z=0.187 önceki turdan korunan mesh-türevli tahmin)
- Dingil mesafesi 780mm, teker aralığı 1060mm, tekerlek yarıçapı 200mm (mekanik ekipten). Tekerlek genişliği (`length=0.12`) ve tekerlek kütlesi/inertia'sı (3.5kg/tekerlek) Zehra'nın orijinal değerleri, dokunulmadı.
- Visual mesh: `meshes/tulpar_ika_assembly_tekersiz.stl` — kullanıcının Desktop'tan tekrar sağladığı `!!tulpar ika assembly.stl` (MD5 doğrulamasıyla teyit edildi: 16 Temmuz'dan beri kullanılan orijinal 149MB/2.987.728-face CAD dosyasıyla bayt-bayt özdeş) temel alınarak üretildi. **Not**: 18 Temmuz 2026'da bir ara Zehra'nın `tulpar_yeni.stl`'i (bkz. `b673c70` commit, daha hafif/1.2M-face export) denendi, kullanıcı görsel olarak eski/hatalı tasarım izlenimi verdiğini belirtip Desktop'taki orijinal dosyayı kullanmaya karar verdi — `tulpar_yeni.stl` repodan çıkarıldı.
- **"Çifte tekerlek" + test çubuğu temizliği tekrarlandı (18 Temmuz 2026)**: orijinal `tulpar_ika_assembly.stl` mesh'i trimesh ile split edilip (65.872 parça) hem 16 Temmuz'da belgelenen 16 parçalık gömülü tekerlek imzası (4 köşe × 4 katman, face sayıları `{7844,4434,4238,616}`, tam eşleşme) hem de hiçbir sensöre/simetrik şasi elemanına karşılık gelmeyen, asimetrik 617mm'lik ince bir "test çubuğu" (muhtemelen eski anten/test artığı — bu artık bilinen bir şey, önceki notlarda yoktu çünkü daha önce fark edilmemişti) çıkarılıp yeni `tulpar_ika_assembly_tekersiz.stl` üretildi (2.919.890 face, eskisinden ~68k face az). Orijinal `tulpar_ika_assembly.stl` dokunulmadan repoda duruyor (pristine referans kopya).
- **Önemli — `git merge` bu iki repo arasında çalışmıyor (18 Temmuz 2026)**: Zehra'nın branch'iyle `git merge origin/feature/zehra-slalom-core` denendi, `fatal: refusing to merge unrelated histories` hatası verdi — iki repo git açısından ortak bir atadan gelmiyor (muhtemelen ayrı `git init`). Bu yüzden bugüne kadarki tüm "Zehra'nın branch'inden adopte edildi" notları hep elle dosya kopyalama/seçici entegrasyon yoluyla yapılmış, gerçek merge değil — ileride de aynı şekilde devam edilmeli, `--allow-unrelated-histories` zorlamak repo yapısını (flat vs. onun nested `src/tulpar_description/...` workspace'i) karıştırıp istenmeyen dosyalar (Docker dosyaları, boş `ika_description` iskeleti, ilgisiz paketler) getirebilir.
- Sensors (hepsi Zehra'nın gerçek Gazebo sensor plugin'leriyle bağlı): YDLIDAR TG30 on `lidar_link` (`gpu_lidar`, topic `/scan`, 720 samples, 30m), RealSense D435if on `d435if_link` (`depth_camera`, topic `/d435if/depth/image_raw` — 16 Temmuz'da topic ismi, 17 Temmuz'da link ismi de Zehra'nın `feature/zehra-slalom-core` branch'iyle hizalandı, bkz. Kamera Routing tablosu), Pi HQ Camera+16mm (atış/nişan) on `rpi_hq_camera_link` (`camera`, topic `/rpi_hq_camera/image_raw`), SJCAM SJ4000 (arka izleme, 180° geriye dönük) on `rear_camera_link` (`camera`, topic `/rear_camera/image_raw`)
- **ÇÖZÜLDÜ — bridge topic uyumsuzluğu (16 Temmuz 2026)**: `launch/gazebo.launch.py`'deki `parameter_bridge` eski topic isimlerini (`/camera/depth` vb.) bekliyordu, URDF'nin gerçek sensör topic'iyle (`/d435i/depth/image_raw`) eşleşmiyordu. Zehra'nın doğruladığı gerçek çalışan isimlere göre hem URDF'nin sensör `<topic>`'i hem bridge güncellendi: `/d435if/depth/image_raw`, `/d435if/depth/camera_info`, `/d435if/depth/image_raw/points`. Gazebo testinde `ros2 topic echo --once` ile üçü de gerçek veri akıtırken doğrulandı. `rtabmap.launch.py`'nin yorumlarındaki eski isimler de güncellendi (kod tarafında zaten kameraya bağlı değildi, sadece dokümantasyon).
- IMU/GPS: mekanik ekibin planına göre AKV (Araç Kanıt Videosu, 20 Temmuz) sonrası entegre edilecek, URDF'de henüz yok.

### Kamera Routing Kararı (16 Temmuz 2026)
Hangi sensörün hangi iş için kullanılacağı — `obstacle_injector`, tabela tespiti gibi ileride yazılacak node'lar için referans:

| Sensör | Link | Topic(ler) | Kullanım amacı |
|---|---|---|---|
| RealSense D435if | `d435if_link` | `/d435if/depth/image_raw`, `/d435if/depth/camera_info`, `/d435if/depth/image_raw/points` | **Derinlik / point cloud** — engel algılama, RTAB-Map'in 3D görsel katmanı. RGB yok, sadece depth. |
| Pi HQ Camera + 16mm lens | `rpi_hq_camera_link` | `/rpi_hq_camera/image_raw` | **Tabela tespiti + atış/nişan** — dar FOV (0.6606 rad), uzak mesafe hedefleme için |
| SJCAM SJ4000 | `rear_camera_link` | `/rear_camera/image_raw` | **Arka izleme** — 180° geriye dönük, geri manevra/izleme |
| YDLIDAR TG30 | `lidar_link` | `/scan` | 2D lazer tarama — RTAB-Map'in birincil kayıt (registration) kaynağı, occupancy grid |

### Launch file (`launch/gazebo.launch.py`)
Starts three nodes:
1. `robot_state_publisher` — processes the xacro and publishes `/robot_description` + TF
2. `gz_sim` — Gazebo Harmonic with `--render-engine ogre`
3. `parameter_bridge` — bridges ROS ↔ Gazebo topics: `/cmd_vel` (ROS→GZ), `/odom`, `/joint_states`, `/scan`, `/d435if/depth/*` (GZ→ROS), `/rear_camera/*`, `/rpi_hq_camera/*` (17 Temmuz 2026'da eklendi — bkz. aşağıdaki not)

**ÇÖZÜLDÜ — `rear_camera`/`rpi_hq_camera` bridge eksikliği (17 Temmuz 2026)**: `urdf/robot.urdf.xacro`'da her ikisinin de çalışan Gazebo `<sensor>` bloğu vardı (Gazebo içinde veri üretiyorlardı) ama bridge listesinde hiç yoktu — ROS2 tarafına hiç sızmıyorlardı. Zehra'nın `feature/zehra-slalom-core` branch'inde bu iki kameranın bridge satırları (+`camera_info`) zaten eklenmişti; sadece `image_raw`/`camera_info` alındı, adopt edildi.

**MİMARİ EKSİKLİK ÇÖZÜLDÜ (16 Temmuz 2026):** Daha önce bu launch dosyasında hiçbir spawn mekanizması yoktu — robot Gazebo'ya sadece `worlds/*.sdf` içine gömülmüş statik bir `<model name='tulpar'>` bloğu olarak giriyordu, `urdf/robot.urdf.xacro`'daki değişiklikler Gazebo'nun simüle ettiği robotu hiç etkilemiyordu. Çözüldü: her 3 world dosyasından (`s01_robotlu_world.sdf`, `s02_hedef_world.sdf`, `e02_duz_world.sdf`) gömülü statik `<model name='tulpar'>` bloğu (içindeki eski `DiffDrive`/`JointStatePublisher` plugin'leriyle birlikte) çıkarıldı; `gazebo.launch.py`'ye `ros_gz_sim create` ile gerçek bir spawn adımı eklendi (`-topic robot_description` üzerinden canlı xacro'dan spawn ediyor, `TimerAction` ile `spawn_delay` argümanı kadar — varsayılan 5sn — Gazebo dünyasının yüklenmesini bekliyor, entity adı `tulpar` sabit tutuluyor çünkü bridge'in `/model/tulpar/tf` remap'i buna bağlı).
- **Test durumu**: sadece **s01 gerçek Gazebo testiyle doğrulandı** (ekran görüntüsü alındı, robot zeminde doğru duruyor, yeni mesh görünüyor, terrain sağlam). **s02 ve e02 aynı model-bloğu-kaldırma düzeltmesini aldı ama test edilmedi** — S02/E02 testlerine dönüldüğünde doğrulanmalı (S01 dünyasındaki gibi).
- **Bilinen kırılganlık — stale process**: Test sırasında gerçek bir sorunla karşılaşıldı: saatler önceki bir launch'tan kalan, hiç kapanmamış bir `robot_state_publisher` süreci hâlâ eski (terk edilmiş) `robot_description`'ı `/robot_description` topic'ine yayınlıyordu; `ros_gz_sim create -topic robot_description` bu ESKİ mesajı yakaladı (ROS2 topic'leri process'e özel değil, global — birden fazla publisher aynı topic'e yayın yapabilir). Sonuç: spawn edilen robot yanlış/eski mesh'lerle görünüyordu. **İleride benzer "spawn edilen robot beklenenden farklı görünüyor" belirtisiyle karşılaşılırsa önce `ps aux | grep robot_state_publisher` ile birden fazla instance çalışıp çalışmadığı kontrol edilmeli** — launch'lar arası eski süreçlerin tam kapatıldığından emin olmadan test tekrarlamak yanıltıcı sonuç verebilir.

### Worlds (`worlds/`)
| File | Purpose |
|------|---------|
| `sim_world.sdf` | **17 Temmuz 2026'dan itibaren varsayılan world.** Zehra'nın `feature/zehra-slalom-core` branch'inden alınan tam parkur: tek bir statik `parkur` modeli altında `zemin`, `yol`, `dik_egim`, `dik_engel`, `yan_egim`, `tasli_cakilli_yol`, `sudan_gecis`, `engebeli_arazi`, `cizgiler`, `bariyer` (track boundary, collision+visual — mekanik ekibin orijinal `20250228_Parkur` referans paketinden ayrıca eklendi, aşağıdaki kritik bug notuna bkz.), `kukalar` (koniler, sadece visual), `tabelalar` (sadece visual) — gerçek mesh'ler (`meshes/parkur/*.stl`, ~201MB, git-lfs). S-01/S-03/S-04'ün çoğu senaryosunu tek world'de kapsıyor. Gazebo'da gerçek testle doğrulandı (17 Temmuz), ekran görüntüsüyle görsel olarak da teyit edildi: hata/uyarı yok, `/scan`/`/odom`/tüm kamera topic'leri gerçek veri akıtıyor. |
| `e02_duz_world.sdf` | E-02 acceleration: flat 30+ m track with distance markers. Hızlanma testi için ayrı, temiz bir düz pist. **Spawn mimarisi 17 Temmuz 2026'da gerçek Gazebo testiyle doğrulandı** (tek temiz instance, hatasız, `/odom`+`/scan` akıyor). |

~~`s01_robotlu_world.sdf`~~, ~~`s02_hedef_world.sdf`~~ **kaldırıldı (17 Temmuz 2026)** — `sim_world.sdf` ile kapsamları örtüştüğü ve S01'in terrain'i zaten 16 Temmuz'da boşaltılmıştı. `s01_robotlu_world_ORIJINAL_YEDEK.sdf.bak` da silindi (zaten `.gitignore`'daydı, hiç commit edilmemişti).

Hiçbir world dosyası artık robotu gömülü içermiyor — robot canlı `urdf/robot.urdf.xacro`'dan `gazebo.launch.py` tarafından spawn ediliyor (bkz. yukarıdaki "MİMARİ EKSİKLİK ÇÖZÜLDÜ" notu). `setup.py`'deki mesh install kuralı `meshes/parkur/` alt klasörünü kapsayacak şekilde ayrı bir `data_files` girdisiyle güncellendi — eski `glob('meshes/*')` alt klasörleri atlıyordu, sessizce mesh eksikliğine yol açardı.

### Test scripts (`test_kanit/`)
Each test is a standalone ROS 2 node that publishes `/cmd_vel` and subscribes `/odom`. Pattern:
1. Wait up to 5 s for `/odom` (abort if none received — check Gazebo is running)
2. Execute movement sequence, collecting timestamped data
3. Write results to a CSV next to the script
4. Companion `*_grafik.py` scripts read the CSV and produce matplotlib plots

| Directory | Tests |
|-----------|-------|
| `E02/` | 30 m straight-line acceleration at 0.5 m/s |

~~`S01/`~~, ~~`S02/`~~ **kaldırıldı (17 Temmuz 2026)** — ikisi de artık var olmayan world'lere göre yazılmıştı (S01 zaten 16 Temmuz'dan beri geçersizdi, S02 basit geometrik simülasyondu). `sim_world.sdf`'in gerçek mesh'leri üzerinden yeni testler yazılacak.

### Behavior Tree (`behavior_trees/`)
`ana_tree.xml` — main Behavior Tree draft (BehaviorTree.CPP v4, `BTCPP_format="4"`, Groot2-openable). Bu BT sadece TAM-OTONOM koşuyu (şartname 1.1 - 2. Koşu) modelliyor; Manuel koşu (1. Koşu, RC/kumanda ile) bu ağacın dışında, ELRS/RC override katmanında yönetiliyor. Sequence: wait for system ready → repeat until course end → ReactiveFallback between three branches: (1) sign-detected shoot flow (stop Nav2, lock target, fire laser, resume Nav2), (2) `DikEgimStopAkisi` — rampadaki işaretli stop noktasında (`StopNoktasindaMi`) tam durup en az 2 sn bekleme (`DurVeBekle`, şartname 6.10), (3) default autonomous driving (Nav2 `NavigateToPose` placeholder). Parkurdaki her tabela BT node'u gerektirmiyor — sadece davranış değişikliği gerektirenler (atış, dik eğim stop) burada modellendi; su geçişi, çakıllı yol, yan eğim, dik engel, trafik konileri, kayar engel ve engebeli arazi gibi diğer bölümler Nav2/costmap parametreleriyle sürekli geçiliyor (Zehra'nın parkur algoritmaları kapsamı, ayrı BT node'u gerektirmiyor). All action/condition nodes now have real C++ implementations in `tulpar_bt/` (see below) — no longer placeholders.

### Behavior Tree Executor (`tulpar_bt/`)
Yeni C++ paketi: `tulpar_bt` (ament_cmake). Symlink kurulumu gerekiyor — bkz. Build & Run bölümündeki "One-time setup" notu.

9 BT node'unun C++ implementasyonu tamamlandı (artık placeholder değiller):
- **TAM İMPLEMENTE**: `WaitForSystemReady`, `OtonomSurus`, `ModDegisimiYap`, `DevamEt`, `StopNoktasindaMi`, `DurVeBekle`
- **YAZILIMSAL TAMAMLANDI** (donanım/ekip verisi bekleniyor): `TabelaAlgılandiMi` (`/sign_detected`, `tulpar_ika_msgs/SignDetectionArray` dinliyor — 17 Temmuz 2026'da eski `/tabela_tespit` `std_msgs/Bool` placeholder'ından taşındı; sadece `action == ACTION_ENTER_SHOOTING_ZONE` [=9, `SignDetection.msg`, stage_12 tabelası — Zehra'nın `feature/zehra-slalom-core` branch'inden 17 Temmuz'da senkronlandı] taşıyan tespit atış akışını tetikliyor, diğer tabelalar [STOP/SPEED_LIMIT/vb.] tetiklemiyor), `HedefTespitYap` ve `AtisYap` (lazer donanımını bekliyor — servis yoksa `simulate_mode` ile simüle edilir; asıl hedef-kilitleme PID'i Emin'in ayrı bir node'unda olacak, bu ikisi sadece `/hedef_tespit_yap`+`/atis_yap` `std_srvs/Trigger` servislerini tetikleyen orkestrasyon katmanı)

`WaitForSystemReady`, Nav2 lifecycle_manager entegrasyonu yapar: `bt_navigator`/`controller_server`/`planner_server`'ın ACTIVE durumda olduğunu `lifecycle_msgs/GetState` ile kontrol eder.

Çalıştırma: `ros2 run tulpar_bt bt_executor` (ya da `ros2 launch tulpar_bt bt_executor.launch.py`)

**Bilinen davranış**: `ana_tree.xml`'deki `Repeat` düğümü `ForceSuccess` ile sarılı — tek bir dal FAILURE dönse bile döngü kalıcı durmuyor (BT.CPP'de `Repeat`'in doğal davranışı child FAILURE'da kalıcı durmaktır). Parkur bitiş koşulu henüz tanımlı değil — Gün 4'te ele alınacak.

`OtonomSurus`'un hedef kaynağı `/tulpar_bt/hedef_pose` topic'i — Zehra'nın parkur algoritması yayınlayacak, henüz bağlı değil.

Gerçek uçtan uca test sahte/mock Nav2 (fake lifecycle node'lar + fake `navigate_to_pose` action server) ile yapıldı ve doğrulandı. Gerçek Gazebo+Nav2 testi Gün 4'te.

### RTAB-Map SLAM (`launch/rtabmap.launch.py`)

Lidar-öncelikli 2D+3D SLAM: `/scan` ile ICP tabanlı (`Reg/Strategy=1`) occupancy grid, RGB kamera gerektirmiyor. Sensör ismi/topic geçmişi: kurulduğunda `camera_link`/RealSense D455/`camera/depth`, Gün 4'te Zehra'nın branch'i temel alınınca `d435i_link`/`/d435i/depth/image_raw`, aynı gün Zehra'nın doğruladığı gerçek çalışan isme (`/d435if/depth/image_raw/points`) güncellendi, 17 Temmuz'da link ismi de `d435if_link`'e taşınarak topic ismiyle hizalandı (bkz. Kamera Routing tablosu) — RTAB-Map kod tarafında zaten kameraya bağlı değil (lidar-only), sadece dokümantasyon/yorum güncellemesi gerekti.

Çalıştırma: `gazebo.launch.py` + ayrı terminalde `rtabmap.launch.py`.

`/camera/depth/points` ayrı 3D görsel katman olarak mevcut, SLAM grafiğine karışmıyor.

`map_always_update:=true` şart — yoksa harita ilk taramadan sonra donuyor.

### Perception Mesaj Sözleşmesi (`tulpar_ika_msgs/`)
Zehra'nın `feature/zehra-faz1-gun1` branch'inden (`~/tulpar_ika_sim_incele/src/tulpar_ika_msgs`) 17 Temmuz 2026'da bu repoya taşındı (ament_cmake, rosidl interface paketi). Aktif algı sözleşmesi:
- **`/detections`** (`tulpar_ika_msgs/msg/Detection2DArray`) — genel obje/koni tespiti, ham perception çıktısı. Her `Detection2D`: `class_id`, `class_name`, `confidence`, `bbox` (`sensor_msgs/RegionOfInterest`), `center_px`, `depth_m`/`depth_valid`, `track_id`/`track_id_valid`, `source` (UNKNOWN/OPENCV/YOLO/FUSED enum). Koni sınıfı: `class_name == "traffic_cone"`. Atış hedefi (halka): `class_name == "atis_hedefi"`, `class_id == 13` — her frame akıtılır, Emin'in ayrı PID node'u bunu okuyacak (`tulpar_bt`'nin kapsamında değil).
- **`/sign_detected`** (`tulpar_ika_msgs/msg/SignDetectionArray`) — tabela tespiti, `TabelaAlgilandiMi`'nin dinlediği gerçek topic (bkz. yukarıdaki BT Executor notu). `action` enum'u artık `ACTION_ENTER_SHOOTING_ZONE=9` içeriyor (17 Temmuz 2026, Zehra'nın `feature/zehra-slalom-core` branch'inden senkronlandı) — `class_registry.yaml`'da (gerçek konumu `tulpar_ika_perception/config/class_registry.yaml`, ilk konuşulduğu gibi `tulpar_ika_msgs/config/` değil) `stage_12` bu action'a, `stop_marking` `ACTION_STOP`'a eşleniyor, diğer tüm stage'ler şimdilik `ACTION_UNKNOWN`.
- `ConeDetection.msg` repoda duruyor ama **aktif sözleşmede kullanılmıyor** — koni verisi `/detections` üzerinden generic `Detection2D` olarak geliyor, ayrı bir koni-özel topic yok. Kafa karıştırmasın diye burada not düşüldü.
- `class_id` değerleri model yeniden eğitilse bile sabit kalacak `class_registry.yaml` 17 Temmuz 2026'da geldi (konumu yukarıda) — ama şu an sadece tabela sınıflarını (`stage_01`..`stage_12`, `stop_marking`) `action` enum'una eşliyor, `traffic_cone`/`atis_hedefi` (id 13) koni/hedef sınıfları için hâlâ ayrı bir kayıt yok, sadece sözlü teyit var.

### Obstacle Injector (`tulpar_obstacle_injector/`)
Yeni Python paketi (ament_python), 17 Temmuz 2026'da yazıldı. `/detections` içindeki `class_name == "traffic_cone"` tespitlerini (`depth_valid == true` olanları) pinhole geri izdüşümü (`center_px` + `depth_m` + `camera_info`'dan `fx/fy/cx/cy`) ile kamera optik frame'inde 3D noktaya çevirir, `tf2_ros` ile `odom`'a taşır, `/parkur/koni_tespitleri` (`geometry_msgs/PoseArray`, `frame_id=odom`) olarak yayınlar. Gelen her `/detections` mesajından taze bir `PoseArray` üretir — geçmiş biriktirmez (Zehra'nın "sadece o an görünen koniler" sözleşmesiyle tutarlı).
- Parametreler: `cone_class_name` (varsayılan `traffic_cone`), `detections_topic` (`/detections`), `camera_info_topic` (`/d435if/depth/camera_info`), `output_topic` (`/parkur/koni_tespitleri`), `target_frame` (`odom`), `tf_timeout_sec`.
- Çalıştırma: `ros2 run tulpar_obstacle_injector obstacle_injector` (ya da `ros2 launch tulpar_obstacle_injector obstacle_injector.launch.py`)
- **Test durumu**: sahte `camera_info` + `/detections` mesajlarıyla duman testi yapıldı (TF ağacı olmadan doğru şekilde WARN loglayıp boş `PoseArray` yayınladığı doğrulandı — çökmüyor). **Gazebo+robot_state_publisher ile gerçek TF ağacı üzerinden uçtan uca henüz test edilmedi** — Gün 5/6'da Gazebo çalışırken doğrulanmalı.
- TEB/Nav2 costmap'e bu `PoseArray`'in nasıl bağlanacağı (costmap obstacle layer mı, TEB'in native obstacle topic'i mi) henüz kararlaştırılmadı — TEB parametreleri işiyle birlikte ele alınacak.

### ⚠️ Zehra'nın `feature/zehra-slalom-core` branch'inden BİLEREK MERGE EDİLMEYENLER (17 Temmuz 2026, güncellendi)
Bu branch, `class_registry.yaml`/`ACTION_ENTER_SHOOTING_ZONE` ile birlikte URDF/bridge değişiklikleri de getiriyor. `d435i_link`→`d435if_link` rename'i ve `rear_camera`/`rpi_hq_camera` bridge eksikliği çözüldü (aşağıda), ama tekerlek geometrisi **hâlâ bilerek merge edilmedi** çünkü bizim repoyla gerçek bir çakışması var — ekip kararı olmadan uygulanmamalı:
1. ~~`d435i_link` → `d435if_link` yeniden adlandırması~~ — **UYGULANDI (17 Temmuz 2026)**: doğru isim olduğu onaylandı (D435if donanım adıyla, bridge topic isimleriyle tutarlı), `urdf/robot.urdf.xacro`'daki tüm link/joint/gazebo-reference isimleri ve `launch/rtabmap.launch.py`'deki yorum güncellendi. `xacro` ile derlenip doğrulandı.
2. **Tekerlek geometrisi çelişkisi**: o branch'te visual radius 0.165→0.10 değişmiş ama collision hâlâ 0.165 (kendi içinde bile tutarsız) — bizim repo ise mekanik ekipten gelen 0.200m değerine göre STL'den gömülü tekerleği çıkarmıştı (bkz. "çifte tekerlek" notu). İki taraf artık uyumsuz. **Hâlâ açık.**
3. ~~`rear_camera_link`'in Gazebo `<sensor>` bloğu silinmişti~~ — **ÇÖZÜLDÜ (17 Temmuz 2026)**: Zehra `274f4f1` commit'inde sensör bloğunu geri ekledi, biz de bridge satırlarını (`image_raw`+`camera_info`, `rpi_hq_camera` ile birlikte) `gazebo.launch.py`'ye aldık.

**ÇÖZÜLDÜ — sensör montaj konumları (17 Temmuz 2026)**: `274f4f1` commit'inde `lidar_joint`/`d435if_joint`/`rpi_hq_camera_joint`'in `origin xyz` değerleri değişmişti (üçü de öne/yukarı toplanmış, muhtemelen gerçek bir sensör direği/braket tasarımı). Kullanıcı onayladı, Zehra'nın değerleri alındı: lidar `0.58 -0.015 0.61`, d435if `0.58 0.0 0.53`, rpi_hq_camera `0.58 -0.03 0.58`. `xacro` ile derlenip doğrulandı — henüz Gazebo'da görsel/FOV testiyle doğrulanmadı.

Ayrıca kamera çözünürlüğü/frekansı genel olarak düşürülmüş (640×480→320×240, lidar 5→2Hz/720→360 örnek, d435if 10→2Hz, rpi_hq 10→1Hz) — hatalı değil ama bilinçli bir karar olarak senkronlanmalı, tesadüfen değil.

~~**Ayrıca fark edildi**: `worlds/parkur/*.stl`... henüz incelenmedi.~~ — **İNCELENDİ VE ALINDI (17 Temmuz 2026)**: `sim_world.sdf` gerçekten tam bir parkur world'ü, gömülü statik robot yok, gerekli 4 world-plugin'i mevcut. `git lfs pull` ile gerçek mesh içeriği çekildi (~201MB, 11 dosya) ve `meshes/parkur/`'a kopyalandı, `sim_world.sdf` yeni varsayılan world oldu, eski `s01_robotlu_world.sdf`/`s02_hedef_world.sdf` ve karşılık gelen `test_kanit/S01`/`S02` kaldırıldı (bkz. Worlds/Test scripts bölümleri). Gazebo'da gerçek testle doğrulandı — hata yok, tüm sensör topic'leri veri akıtıyor.

**⚠️ KRİTİK BUG BULUNUP DÜZELTİLDİ (17 Temmuz 2026, aynı gün) — mesh dosyaları sistematik olarak yanlış isimlendirilmişti**: Kullanıcı, mekanik/organizasyon ekibinden gelen orijinal referans paketini (`20250228_Parkur/`, STL+OBJ+DAE+STEP, 12 parça — 11 bizim setimizle aynı isimler + eksik olan `bariyer`) paylaşınca, MD5 karşılaştırmasıyla doğrulandı: Zehra'nın verdiği 11 dosyanın **her biri bir kayma ile yanlış isimlendirilmişti** — örn. bizim `kukalar.stl` (koni) aslında `engebeli_arazi` (engebeli arazi) içeriğiydi, bizim `tabelalar.stl` (tabela) aslında `sudan_gecis` (su geçişi) içeriğiydi, bizim `cizgiler.stl` aslında `bariyer` (bariyer) içeriğiydi, ve gerçek `zemin.stl` (asıl zemin) setimizde hiç yoktu. Bu, Gazebo'da hiçbir hata/uyarı vermeyen türden bir bug'dı (geometri yükleniyor, sadece link ismiyle içerik uyuşmuyordu) — ilk testin "hatasız" geçmesi içerik doğruluğunu garantilemiyordu. **Düzeltme**: resmi `20250228_Parkur/20250228/STL/` klasöründeki doğru etiketli 12 dosya (11 düzeltme + yeni `bariyer.stl`) `meshes/parkur/`'a kopyalandı (üzerine yazıldı), `sim_world.sdf`'e yeni bir `bariyer` link'i eklendi (collision+visual, `zemin` linkiyle aynı `pose`/`scale` kalıbı — bbox analiziyle aynı koordinat sistemini paylaştığı doğrulandı: bariyer bbox'ı yol'unkiyle örtüşüyor). Gazebo'da tekrar test edildi, hata yok, ekran görüntüsüyle görsel olarak da S-şekilli parkur doğrulandı (materyal/doku hiçbir link'te tanımlı değil, hepsi düz gri/beyaz render ediliyor — kozmetik, ayrı iş). **Ders**: bir mesh setinin "yüklenmesi" içeriğinin doğru olduğu anlamına gelmez — çapraz kaynak (bu durumda resmi orijinal paket) olmadan böyle bir isim-kayması sessizce fark edilmeyebilir.

### Slalom Karar Çıktısı (`tulpar_ika_perception/slalom_core.py`, Zehra — henüz repoya alınmadı)
Zehra'nın `feature/zehra-slalom-core` branch'inde saf Python mantığı olarak duruyor (ROS2 node'u yok, sadece `slalom_debug.py` ile elle test ediliyor). `run_slalom_logic()` sol/sağ koni piksel konumlarından bir `target_point` (piksel x,y — 3D/odom pose DEĞİL), `turn_direction` (string: left/right/straight/unknown) ve `speed_level` (string: fast/medium/slow/stop) üretiyor. Kendi notunda (`docs/slalom_algorithm_notes.md`) hâlâ "Navigation tarafı obstacle mı waypoint mi corridor mu bekliyor?" sorusu açık duruyor.

**Talha'nın önerisi (henüz onaylanmadı)**: bu bir *waypoint* — zaten var olan `/tulpar_bt/hedef_pose` (`PoseStamped`, `OtonomSurus` dinliyor) hedefi. Zehra'nın piksel `target_point`'ini `tulpar_obstacle_injector`'daki gibi derinlik+TF ile odom'a çevirip oraya yayınlaması gerekiyor. `speed_level` için `PoseStamped`'de yer yok — ya bırakılıp Nav2/TEB'in kendi hız limiti kullanılacak ya da ayrı küçük bir topic (`/parkur/hiz_seviyesi`) açılacak. `turn_direction` ayrı taşınmaya gerek yok, pose'un pozisyonundan çıkarılabilir.
- TEB/Nav2 costmap'e bu `PoseArray`'in nasıl bağlanacağı (costmap obstacle layer mı, TEB'in native obstacle topic'i mi) henüz kararlaştırılmadı — TEB parametreleri işiyle birlikte ele alınacak.

Bu görevde 4 gerçek altyapı bug'ı bulunup düzeltildi: `GZ_SIM_RESOURCE_PATH` eksikliği (dünya hiç yüklenmiyordu), 3 world SDF'sinde eksik world-seviyesi Gazebo plugin'leri (sensörler sessizce veri üretmiyordu), `odom`→`base_footprint` TF köprüsü eksikliği, `ros-jazzy-diagnostic-updater` ABI uyumsuzluğu (`apt upgrade` ile çözüldü).

`e02_duz_world.sdf`'ye aynı plugin düzeltmesi uygulandı, 17 Temmuz 2026'da test edildi ve doğrulandı (`s02_hedef_world.sdf` artık mevcut değil, `sim_world.sdf` ile değiştirildi).

### Yer İstasyonu (`yer_istasyonu/`)
Electron + React + rclnodejs ile yazılmış masaüstü kontrol konsolu. Main process'te rclnodejs.init() ile bir ROS 2 node'u ("yer_istasyonu_node") oluşturuluyor ve DDS ağına katılıyor. Gün 2'de Emin'in KTR madde 3.3.1 görevi olan telemetri paneli ve WebRTC video alıcı eklendi (normalde Emin'in kapsamı, Talha üstlendi):

- **Telemetri paneli**: hız/batarya/sıcaklık/eğim göstergeleri, tek birleşik topic üzerinden besleniyor: `/telemetri/veri` (`std_msgs/String`, JSON payload: `hiz`, `batarya_yuzde`, `sicaklik_c`, `egim_derece`, `zaman_damgasi`).
- **WebRTC video alıcı**: RTCPeerConnection tabanlı, signaling server URL'i `config.js`'ten alınıyor (env: `VITE_SIGNALING_URL`).
- **Durum etiketleri**: UI, IPC köprüsü, rclnodejs subscriber mantığı ve WebRTC peer/negotiation mantığı **TAM İMPLEMENTE**. `/telemetri/veri`'ye yayın yapan gerçek Teensy/Jetson köprüsü (Gün 6'da gelecek) ve WebRTC signaling server (Zehra/Emin'in Gün 4 işi) henüz yok — bu ikisi **YAZILIMSAL TAMAMLANDI - bekleniyor**. Veri/bağlantı gelmezse panel crash etmeden "veri yok/bağlantı bekleniyor" gösterip sürekli yeniden dener.
- **Düzeltilen bug**: preload script'i `package.json`'daki `"type":"module"` yüzünden electron-vite tarafından ESM (`.mjs`) olarak build ediliyordu; Electron'un sandboxed preload yükleyicisi ESM `import` syntax'ını desteklemiyor, bu yüzden preload sessizce yüklenemiyor ve `window.api` hep `undefined` kalıyordu (Gün 1'de fark edilmemişti çünkü preload boştu). `electron.vite.config.mjs`'e preload için CJS çıktı zorunluluğu eklenerek düzeltildi.

Çalıştırma: `cd yer_istasyonu && npm start`
Bilinen sorun: bazı Linux ortamlarında Electron sandbox izin hatası çıkabilir (`SUID sandbox helper binary...`) - çözüm: `node_modules/electron/dist/chrome-sandbox` dosyasını root:root/4755 yap, veya `npm start -- --noSandbox` kullan.

## Proje Bağlamı

- **Yarışma**: TEKNOFEST 2026 İnsansız Kara Aracı (İKA) — Araç: TULPAR
- **Kullanıcı**: Talha Dağ — Otonom Akış & Entegrasyon Sorumlusu; yazılım ekibi: Emin (görüntü işleme), Zehra (parkur algoritmaları / haberleşme)
- **Sprint**: 13–27 Temmuz 2026 (15 günlük yoğun uygulama sprinti)
- **KRİTİK DEADLINE**: Araç Kanıt Videosu (AKV) teslimi **20 Temmuz 2026 17:00** (şartname §4.4) — bu tarihe yaklaşan görevlerde uyar
- **Talha'nın görev alanları**: BT (Behavior Tree) mimarisi, Nav2 entegrasyonu, yer istasyonu (Electron + React + rclnodejs), EKF / sensör füzyonu, Watchdog/Failsafe, ELRS RC override, ağ altyapısı (Bullet M5)
- **Git kuralı**: Her zaman `talha_gelistirme` branch'inde çalış; `main` ve `yazilim_gelistirme`'ye direkt commit atma. Gün sonunda commit + push + PR'ı hatırlat.
- **Test kodları ↔ parkur eşleşmesi**: S-01 (arazi), S-02 (hedef tespit), S-03 (su geçişi), S-04 (slalom/kayar engel), S-05 (tam parkur), E-02 (hızlanma), E-03 (BMS), E-04 (failsafe), B-04 (boyut kontrolü)
- **Donanım**: Jetson Orin NX + Teensy 4.1 (I2C → 4× Lityumsan e-bike controller, bkz. Motor Sürücü Mimarisi v2), RPLidar S2, RealSense D435if (**tedarikte yok** — takip edilmesi gereken açık risk), WT901C-RS485 IMU, ZED-F9P RTK GNSS

### Motor Sürücü Mimarisi (v2 - 14 Temmuz güncellemesi)

- **Motor sürücü değişikliği**: Dual VESC 75100 + CAN bus planı terk edildi. Yerine: 4x Lityumsan 48V 22A e-bike controller (analog gaz girişi, dijital/CAN arayüzü yok, tork kontrolü yok, telemetri yok)
- **Yeni kontrol zinciri**: Teensy 4.1 → I2C → TCA9548A mux (0x70) → 4x PCF8591 DAC (0x48) → OP291 yükseltici → sürücü gaz teli. I2C bus hızı 100 kHz sabit (PCF8591 400 kHz desteklemiyor)
- **Gaz hattı kalibrasyon referansı**: DAC=0 → <0.1V, DAC=255 → 4.1–4.2V (multimetre/osiloskopla doğrulanır)
- **Kritik güvenlik riski**: PCF8591'in watchdog'u yok — Teensy donarsa DAC son değerde asılı kalır; bu yüzden Teensy'nin donanım WDT'si açılmalı, WDT reset'inde `setup()` gazları otomatik sıfırlamalı
- **Kanal eşlemesi**: 0=Ön-Sol, 1=Ön-Sağ, 2=Arka-Sol, 3=Arka-Sağ
- `/odom` hâlâ sadece enkoder+IMU'dan geliyor (VESC'ten hiç veri alınmıyordu zaten, bu değişmedi)
- Geri hareket ve nokta dönüş artık ayrı röle + durum makinesi gerektiriyor (motor tam durmadan röle değiştirilemez)
- **Yön değişim prosedürü (zorunlu sıra)**: `DAC=0` yaz → enkoderden hız ≈0 doğrula (yoksa 300ms bekle) → geri rölesini değiştir → 50ms bekle → gaz ver
- **Kademeli fren**: DAC değişim hızı sınırlanır (maks 5 birim/10ms) + fren teli rölesi — VESC regen fren artık yok
- **Pin haritası**: geri vites röleleri pin4 (sol) / pin5 (sağ), kilit rölesi pin2 (PC817+BC337); ELRS alıcı Serial7 (RX=pin28, TX=pin29, CRSF, 420000 baud)
- **Failsafe zamanlaması**: CRSF'den 400ms paket gelmezse → 4 kanala DAC=0 + kilit rölesi LOW
- Sürücüden telemetri yok — teşhis için Teensy, DAC+enkoder verisini 10Hz'de Jetson'a loglamalı
- Anti-windup PID ayarı şart (sürücünün iç rampası nedeniyle entegral birikir)
- `setup()`'ta İLK İŞ her zaman 4 kanala DAC=0 yazmak (güvenlik kuralı)
- **Etkilenen günler**: Gün 2 (I2C/DAC bring-up, VESC Tool artık yok), Gün 6 (CAN kısmı kalktı, PID/enkoder/EKF aynı), Gün 7 (yeni: geri vites/nokta dönüş durum makinesi), Gün 8 (kademeli fren artık yazılımsal DAC azaltma), Gün 11 (kalibrasyon süreci farklı)
- Güncel detaylı plan: `talha_yol_haritasi_v2.md` (repo dışında, Talha'da duruyor — gerekirse içeriğini iste)

## Key Physical Parameters
(`urdf/robot.urdf.xacro`'daki `DiffDrive` plugin'inden doğrulandı, 16 Temmuz 2026 — Zehra'nın orijinal değerleri, henüz mekanik/motor ekibiyle netleştirilmedi)
- Wheel separation: 1.06 m; wheel radius: 0.200 m
- Max linear velocity: 1.0 m/s; max angular velocity: 3.0 rad/s
- Max wheel torque: 25.0 Nm; max linear acceleration: 2.0 m/s²
- Physics: ODE solver, 1 ms step, 1000 Hz update rate, 100 iterations

## Gün Bazlı İlerleme Notları

- Gün 1 (14 Temmuz 2026) tamamlandı: BT taslağı (behavior_trees/), yer istasyonu iskeleti (yer_istasyonu/), repo temizliği ve worlds/ install bug düzeltmesi, PR #1 açıldı ve yazilim_gelistirme'ye merge edildi.
- Gün 2 (14 Temmuz 2026 - devam) BT node implementasyonu tamamlandı: tulpar_bt paketi, 9 node (6 tam implemente, 3 yazılımsal tamamlandı/donanım+ekip bekliyor), 3 gerçek sorun bulunup çözüldü (colcon paket keşfi, Nav2 çökme riski, Repeat kalıcı ölüm riski).
- Gün 2 (devam) Emin'in KTR 3.3.1 görevi (telemetri paneli + WebRTC video alıcı) üstlenildi ve tamamlandı, ayrıca Gün 1'den kalma gizli bir preload ESM/CJS bug'ı bulunup düzeltildi.
- Gün 3 (devam) RTAB-Map SLAM kuruldu ve gerçek Gazebo testiyle doğrulandı (harita oluşumu rviz'de teyit edildi), 4 gerçek altyapı bug'ı bulunup düzeltildi.
- Gün 4 (16 Temmuz 2026) URDF v3.0: yeni CAD assembly'den (`!!tulpar+ika+assembly.stl`, 16.000 parça) gövde+tekerlek mesh'leri çıkarıldı, decimate edildi, kütle/inertia hesaplandı, URDF tamamen güncellendi (yeni boyutlar, 3 yeni sensör linki). RViz ile görsel doğrulama yapıldı, Gazebo fizik testi mimari eksiklik (spawn_entity yok) nedeniyle YAPILAMADI — ayrı görev olarak bekliyor. Süreçte 3 gerçek hesaplama hatası bulunup düzeltildi: negatif eylemsizlik momenti (winding tutarsızlığı), ön/arka işaret hatası (sensör konumları ters çıkıyordu), ve mesh-türevli inertia'nın üçgen eşitsizliğini ihlal etmesi (analitik kutuya geçildi).
- Gün 4 (devam) **Strateji pivotu**: mekanik ekip kendi orijinal `tulpar_ika_assembly.stl`'ini Blender'da inceleyip eksiksiz olduğunu doğruladı — sorun bizim decimation pipeline'ımızdaymış. Kendi mesh yaklaşımımızdan (govde/teker v2/v3, 5 dosya) vazgeçildi, silindi. Onun yerine Zehra'nın `feature/zehra-faz1-gun1` branch'indeki `urdf/robot.urdf.xacro` + ham `tulpar_ika_assembly.stl` (149MB) temel alındı, sadece fiziksel parametreler (1200×450×245mm/64kg/780mm/1060mm/200mm + yeniden hesaplanan inertia) mekanik ekipten gelen değerlerle güncellendi. Sensör link isimleri Zehra'nınkiyle birleşti (`d435i_link`, `rear_camera_link`) — kendi placeholder linklerimizden (`d435if_link`, `sjcam_link`) vazgeçildi çünkü Zehra'nınkiler zaten gerçek Gazebo sensor plugin'lerine bağlıydı. Yeni bilinen sorun: `gazebo.launch.py`'nin bridge'i henüz yeni sensör topic isimleriyle güncellenmedi.
- Gün 4 (devam) **Spawn mimarisi eksikliği kapatıldı**: `worlds/*.sdf`'lerdeki statik gömülü `tulpar` modeli kaldırıldı, `gazebo.launch.py`'ye `ros_gz_sim create` ile canlı-URDF-spawn eklendi. S01 gerçek Gazebo testiyle doğrulandı (ekran görüntüsü alındı); S02/E02 aynı düzeltmeyi aldı ama test edilmedi. Süreçte gerçek bir stale-process bug'ı bulundu ve not düşüldü (birden fazla `robot_state_publisher` aynı `/robot_description` topic'ine yayın yapınca `create` yanlış/eski olanı yakalayabiliyor). Bridge topic uyumsuzluğu artık teorik değil aktif bir sorun — Gazebo gerçekten yeni URDF'yi simüle ettiği için kamera topic'leri muhtemelen akmıyor, ayrı görev olarak açık kaldı.
- Gün 4 (devam) **Kapsamlı denetim + düzeltmeler**: Repo geneli denetlendi, 3 kritik/1 önemli bulgu tespit edilip düzeltildi — CLAUDE.md'nin Worlds bölümü spawn mimarisi notuyla çelişiyordu (eski/statik anlatım silindi), Overview ve Key Physical Parameters bölümleri aylardır güncellenmemiş eski değerler taşıyordu (64kg/1200×450×245mm/Ø400mm ve gerçek DiffDrive değerleriyle güncellendi), `package.xml`'de `xacro` bağımlılığı eksikti (eklendi), `meshes/tulpar_yeni.stl` (61MB, düz git blob) artık hiçbir yerden referans edilmiyordu (silindi).
- Gün 4 (devam) **S01 terrain temizliği**: `worlds/s01_robotlu_world.sdf`'deki 61 terrain modeli (rampa_asc/desc ×20+20, rampa_tepe, yan_giris/cikis/egim ×10, blok_giris/15cm ×4, tumsek ×6) kaldırıldı, world artık sadece `ground_plane` içeriyor. Silmeden önce `worlds/s01_robotlu_world_ORIJINAL_YEDEK.sdf.bak` yedeği alındı (`.gitignore`'a eklendi, repoya commit edilmiyor). **Sonuç: `test_kanit/S01/` scriptleri artık geçersiz** (test ettikleri rampa/blok/tümsek engelleri artık world'de yok) — bkz. Test scripts notu. Gazebo testiyle doğrulandı: araç boş düz zeminde doğru görünüyor.
- Gün 4 (devam) **Çifte tekerlek düzeltmesi + bridge topic güncellemesi**: `tulpar_ika_assembly.stl`'den sadece gömülü eski tekerlek geometrisi (16 parça, kesin yüzey-imzası eşleştirmesiyle) çıkarılıp `tulpar_ika_assembly_tekersiz.stl` üretildi, URDF buna yönlendirildi — gövdeye dokunulmadı, decimation yok. İlk denemede gevşek filtre 8 fazladan yapısal parçayı da yanlışlıkla çıkarmıştı, fark edilip düzeltildi. Ayrıca Zehra'nın doğruladığı gerçek D435if bridge topic isimleri (`/d435if/depth/image_raw` vb.) hem URDF sensör tanımına hem `gazebo.launch.py` bridge'ine işlendi — daha önce URDF'nin kendi topic'i (`/d435i/...`) ile bridge'in beklediği isim (`/camera/...`) birbirinden tamamen kopuktu, ikisi de düzeltildi. Gazebo testiyle doğrulandı: tekerlekler artık çakışmadan oturuyor, 3 kamera topic'i de gerçek veri akıtıyor. CLAUDE.md'ye kamera routing tablosu eklendi.
- Gün 5 (17 Temmuz 2026) **Perception sözleşmesi netleşti, obstacle_injector yazıldı**: Zehra/Emin ile `/detections` (`Detection2DArray`, koni=`traffic_cone`, atış hedefi=`atis_hedefi`/id 13) ve `/sign_detected` (`SignDetectionArray`) sözleşmesi netleşti. `tulpar_ika_msgs` paketi (Zehra'nın `feature/zehra-faz1-gun1` branch'inden) repoya taşındı. Süreçte gerçek bir bug bulundu: `TabelaAlgilandiMi` hâlâ eski placeholder `/tabela_tespit` (`std_msgs/Bool`) dinliyordu — gerçek sözleşmeye (`/sign_detected`, `SignDetectionArray`) taşındı. Yeni `tulpar_obstacle_injector` paketi (Python) yazıldı: `/detections`'taki koni tespitlerini derinlik+TF ile `odom` frame'inde 3D konuma çevirip `/parkur/koni_tespitleri` (`PoseArray`) yayınlıyor; sahte veriyle duman testi geçti (TF yokken çökmeden WARN logluyor), Gazebo'nun gerçek TF ağacıyla uçtan uca henüz test edilmedi. Açık kalanlar: Zehra'nın ekleyeceği `ACTION_ENTER_SHOOTING_ZONE` enum değeri ve `class_registry.yaml` henüz pakette yok; `/parkur/koni_tespitleri`'nin TEB/Nav2 costmap'e nasıl bağlanacağı (obstacle layer vs TEB native obstacle topic) henüz kararlaştırılmadı.
- Gün 5 (devam) **`feature/zehra-slalom-core` senkronu**: Zehra bu branch'i push etti — `ACTION_ENTER_SHOOTING_ZONE=9` (`SignDetection.msg`) ve `class_registry.yaml` (`tulpar_ika_perception/config/`, stage_12→ACTION_ENTER_SHOOTING_ZONE, stop_marking→ACTION_STOP) senkronlandı; `TabelaAlgilandiMi` artık sadece bu action'ı tetikliyor (TODO kapandı). Aynı branch'te URDF/bridge tarafında 3 gerçek çakışma bulundu ve **bilerek merge edilmedi** (bkz. yukarıdaki "BİLEREK MERGE EDİLMEYENLER" bölümü): `d435i_link`→`d435if_link` rename, tekerlek geometrisi çelişkisi, `rear_camera` için sensörsüz bridge (silik bug). Ayrıca Zehra'nın `slalom_core.py`'ının (henüz ROS2'ye bağlı değil, saf Python) çıktısının `/tulpar_bt/hedef_pose`'a nasıl bağlanacağına dair bir öneri geliştirildi, henüz onaylanmadı.
- Gün 5 (devam) **`d435i_link` → `d435if_link` rename uygulandı**: kullanıcı onayladı ("if olması lazım zaten" — D435if donanım adıyla ve zaten var olan bridge topic isimleriyle [`/d435if/depth/...`] tutarlı hale geldi). `urdf/robot.urdf.xacro`'daki tüm ilgili link/joint/gazebo-reference isimleri (`d435i_link`, `d435i_joint`, `d435i_color_frame`, `d435i_color_joint`, `d435i_color_optical_frame`, `d435i_color_optical_joint`, `d435i_depth_frame`, `d435i_depth_joint`, `d435i_depth_optical_frame`, `d435i_depth_optical_joint`, `gazebo reference="d435i_link"`, `sensor name="d435i_depth"`) `d435if_` önekine taşındı; artık-gereksiz açıklama yorumu silindi. `launch/rtabmap.launch.py`'deki yorum güncellendi. `xacro urdf/robot.urdf.xacro` ile derlenip TF ağacının bozulmadığı doğrulandı. Kalan çakışma (tekerlek geometrisi) hâlâ bilerek merge edilmedi.
- Gün 5 (devam) **Tam parkur world'ü alındı, eski S01/S02 kaldırıldı**: Zehra'nın `feature/zehra-slalom-core`'daki `sim_world.sdf`'i (zemin/yol/dik_egim/dik_engel/yan_egim/taşlı-çakıllı yol/sudan_geçiş/engebeli_arazi/çizgiler/kukalar/tabelalar, tek statik `parkur` modeli, gömülü robot yok) incelendi, `git lfs pull` ile gerçek mesh içeriği (~201MB) çekilip repoya alındı. `setup.py`'nin mesh install kuralı alt klasörleri atlıyordu (sessiz eksiklik riski), düzeltildi. Gazebo'da gerçek testle doğrulandı: hata/uyarı yok, tüm topic'ler (`/scan`, `/odom`, 3 kamera) gerçek veri akıtıyor. `sim_world.sdf` yeni varsayılan world oldu; `s01_robotlu_world.sdf`/`s02_hedef_world.sdf` ve `test_kanit/S01`/`S02` (zaten geçersiz/basit-simülasyon idi) kaldırıldı. `e02_duz_world.sdf`/`test_kanit/E02` bilerek dokunulmadı (hızlanma testi için ayrı temiz pist olarak kalıyor). Süreçte bilinen "stale process" kalıbının küçük bir tekrarı bulundu: önceki bir smoke testten kalma `obstacle_injector` süreci saatlerdir arka planda çalışıyormuş, fark edilip öldürüldü.
- Gün 5 (devam) **`rear_camera`/`rpi_hq_camera` bridge eksikliği kapatıldı**: Zehra `feature/zehra-slalom-core`'da `274f4f1` commit'iyle `rear_camera_link`'in sildiği sensör bloğunu geri ekledi (bridge satırları zaten önceki commit'te vardı). Bizim tarafta ayrı bir gerçek eksiklik bulundu: her iki kameranın URDF'de çalışan sensör bloğu vardı ama `gazebo.launch.py` bridge listesinde hiç yoktu — ROS2'ye hiç sızmıyorlardı. Zehra'nın bridge satırları (`image_raw`+`camera_info`, ikisi için) adopt edildi. Aynı commit'te lidar/d435if/rpi_hq_camera'nın mount origin'leri de değişmiş (muhtemelen mekanik direk/braket güncellemesi) — henüz teyit edilmedi, uygulanmadı.
- Gün 5 (devam) **KRİTİK: Parkur mesh dosyaları sistematik yanlış isimlendirilmiş bulundu ve düzeltildi**: Kullanıcı mekanik ekibin orijinal referans paketini (`20250228_Parkur/`) paylaştı; MD5 karşılaştırmasıyla Zehra'nın verdiği 11 mesh dosyasının her birinin bir kayma ile yanlış isimlendirildiği doğrulandı (örn. `kukalar.stl`=koni aslında `engebeli_arazi` içeriğiydi, `tabelalar.stl` aslında `sudan_gecis` içeriğiydi, `cizgiler.stl` aslında `bariyer` içeriğiydi) ve gerçek `zemin.stl` + `bariyer.stl` hiç yoktu. Gazebo'da hatasız yüklenen bir world'ün içerik olarak doğru olduğu anlamına gelmediğinin somut örneği. Resmi pakedeki 12 doğru-etiketli dosya (11 düzeltme + yeni `bariyer`) `meshes/parkur/`'a kopyalandı, `sim_world.sdf`'e `bariyer` link'i (collision+visual) eklendi — bbox analiziyle aynı koordinat sistemini paylaştığı doğrulandı. Gazebo'da tekrar test edildi, ekran görüntüsüyle görsel olarak da S-şekilli parkur teyit edildi.
- Gün 5 (devam) **Parkur mesh'lerine materyal/renk eklendi**: Kullanıcı sordu — "neden hepsi gri, arkadaşın attığında renkliydi" (bkz. reference PNG'ler `20250228_Parkur/`). Kontrol edildi: STL formatı hiç renk taşımıyor, `sim_world.sdf`'teki 12 parkur linkinin hiçbirinde `<material>` yoktu (Zehra'nın world'ünde hiç eklenmemiş). Aynı resmi pakette OBJ/MTL export'u kontrol edildi: **8 parça için gerçek Rhino diffuse rengi kurtarıldı** (kukalar=turuncu, yol=kahverengi, dik_egim/dik_engel/yan_egim=gri, engebeli_arazi=açık gri, sudan_gecis=mavimsi gri, cizgiler=beyaz), **3 parça için renk/doku export'ta da kayıptı** (bariyer, zemin, tasli_cakilli_yol — muhtemelen Rhino'da desen/doku kullanıyorlardı, STL/OBJ/DAE hiçbiri taşımamış) makul yaklaşık renk verildi (bariyer=kırmızı, zemin=kahverengi toprak, tasli_cakilli_yol=gri çakıl), **tabelalar** için gerçek görsel doku var (`Artboard 1.jpg`) ama STL'de UV koordinatı olmadığından haritalanamıyor, açık gri verildi. `sim_world.sdf`'in 12 linkine de `<material>` (ambient/diffuse/specular) eklendi. Gazebo'da ekran görüntüsüyle doğrulandı: kırmızı S-şekilli bariyer + kahverengi zemin net görünüyor, referans PNG'lerdeki genel görünümle tutarlı. Gerçek HSV-kalibreli koni rengi hâlâ Zehra'nın kendi görevi (kendi notunda "henüz sıraya alınmadı" diye işaretli) — buradaki turuncu geçici/başlangıç değeri.
- Gün 5 (devam) **KRİTİK: tekerlek yarıçapı gövdeyle orantısız bulundu, düzeltildi (kendi ölçümümüzle, Zehra'nın branch'ine bağımlı kalmadan)**: Kullanıcı Gazebo'da tekerleklerin gövdeye göre çok büyük/orantısız durduğunu fark etti. Ekran görüntüsüyle doğrulandı: gövde ince bir iskelet gibi, tekerlekler onu yutuyordu. Kök neden: `wheel_radius=0.200` (URDF primitive silindir + DiffDrive plugin) hiçbir zaman mekanik ekiple netleşmiş bir değer değildi (bkz. Key Physical Parameters notu: "Zehra'nın orijinal değerleri, henüz mekanik/motor ekibiyle netleştirilmedi"). Oysa mekanik ekibin doğruladığı gerçek CAD montajından (`tulpar_ika_assembly.stl`) "çifte tekerlek" düzeltmesi sırasında (Gün 4) gömülü eski tekerlek geometrisi ölçülmüştü: **~166mm yarıçap** — bu, elimizdeki tek gerçekten CAD-doğrulanmış tekerlek ölçüsü. `wheel_radius` 0.200→0.166 olarak değiştirildi (primitive silindir visual+collision, DiffDrive plugin, `base_joint`'in z-offset'i). Gazebo'da tekrar test edildi, ekran görüntüsüyle orantının düzeldiği doğrulandı.
- Gün 5 (devam) **E02 spawn testi doğrulandı**: `e02_duz_world.sdf` ile `gazebo.launch.py` (0.166 tekerlek yarıçapıyla) test edildi, tek temiz instance, hata yok, `/odom`+`/scan` gerçek veri akıtıyor. Süreçte ciddi bir bulgu: bu oturumda birden fazla kez `pkill -f` ile "temizlendi" sanılan Gazebo/bridge/robot_state_publisher süreçleri aslında ölmemiş — 6 ayrı launch yığını (16:06'dan beri) art arda birikmiş, `pkill` sonrası `ps aux | grep` doğrulaması yanıltıcı şekilde "temiz" görünüyordu. PID bazlı `kill -9` ile hepsi tek tek temizlendi. **Ders**: bundan sonra `pkill -f` ile "temizlendi" denip hemen bir sonraki teste geçilmemeli — birkaç saniye sonra `ps aux` ile PID bazlı yeniden doğrulanmalı, güvenilmiyorsa PID listesiyle `kill -9` yapılmalı.
- Gün 5 (devam) **Tekerlek-gövde oran testi tekrarlandı, plan çıkarıldı**: Kullanıcının isteğiyle E02 testi + sim_world.sdf için yeni test scriptleri + Zehra'ya gidecek dosya listesi için plan sunuldu. E02 testi tamamlandı (yukarıda). Test scriptleri: TEB/Nav2 kurulana kadar otonom tam-parkur testi anlamsız, elle cmd_vel ile parça parça doğrulama scripti önerisi sunuldu, henüz yazılmadı. Zehra dosya listesi: asıl doğru yolun PR açmak olduğu belirtildi (branch hâlâ mergesiz), elle paylaşım gerekirse liste hazır (`urdf/robot.urdf.xacro`, `meshes/parkur/*`, `worlds/sim_world.sdf`, `launch/gazebo.launch.py`, `meshes/tulpar_ika_assembly*.stl`, `CLAUDE.md`).
- Gün 6 (18 Temmuz 2026) **Gövde mesh'i üzerinde iki tur revizyon, sonunda orijinal CAD dosyasında karar kılındı**: Zehra `feature/zehra-slalom-core`'a önce eski tekerlek yarıçapı/kütle değerlerine (r=0.20, 65kg) geri dönen bir regresyon commit'i (`08c9cba`) attı, sonra aynı gün gövdeyi yeni bir mekanik CAD export'una (`tulpar_yeni.stl`, `b673c70`, 61MB/1.2M-face) geçirdi. Süreçte: (1) `git merge` denendi, `unrelated histories` hatasıyla reddetti (yukarıdaki nota bkz.) — elle dosya adaptasyonuna devam edildi; (2) trimesh ile eski `tulpar_ika_assembly.stl` analiz edilip 65.872 parçaya bölündü, hiçbir sensöre/simetrik şasi elemanına karşılık gelmeyen 617mm'lik ince bir "test çubuğu" bulundu (muhtemelen eski anten/test artığı, önceki notlarda hiç yoktu); (3) mavi kutu (`d435if_link`) ve siyah silindir (`lidar_link`/`rpi_hq_camera_link` lens'i üst üste) gerçek/çalışan sensör tanımları olduğu, silinemeyeceği netleştirildi — sensör montaj konumlarının hiçbir yerde mekanik ekipten doğrulanmış belge olmadığı da teyit edildi (zaten "doğrulanmamış" işaretliydi), Zehra artık mekanikten aldığını söylüyor ama henüz Gazebo'da FOV testiyle teyit edilmedi; (4) `tulpar_yeni.stl` ilk adopte edildi (tekerlek/kamera parametreleri bizimki kalarak), **ama kullanıcı görsel olarak yanlış/eski tasarım izlenimi verdiğini belirtip Desktop'tan kendi elindeki `!!tulpar ika assembly.stl`'i verdi** — MD5 karşılaştırmasıyla bunun **zaten kullandığımız orijinal `tulpar_ika_assembly.stl` ile bayt-bayt özdeş** olduğu ortaya çıktı (yani "yeni" sanılan Zehra'nın dosyasıydı, kullanıcının verdiği aslında bilinen eski/orijinal dosyaydı) — kullanıcıya bildirildi, yine de bu dosyayla devam edilmesi istendi. Sonuç: `tulpar_yeni.stl` repodan çıkarıldı, orijinal `tulpar_ika_assembly.stl` geri kondu, ondan (2) adımındaki 16-parça tekerlek imzası + test çubuğu çıkarılarak yeni bir `tulpar_ika_assembly_tekersiz.stl` üretildi (bkz. yukarıdaki not), xacro bu son dosyaya bağlandı. Ayrıca `test_kanit/E02/*` ve `worlds/e02_duz_world.sdf` oturum içinde disk'ten kayboldu (git'te hâlâ vardı, `git restore` ile geri getirildi) — kullanıcı bunları kendisinin bilerek sildiğini belirtti (eski test kalıntısı), commit'te tekrar silinecek. Gazebo'da spawn+topic testiyle doğrulandı (hata yok, tüm topic'ler akıyor), gövde-tekerlek hizası kullanıcının kendi ekranında görsel olarak kontrol edildi. Commit `43a04ca`, push edildi.
- Gün 6 (devam) **Zehra üçüncü kez aynı mesh'i farklı isimle gönderdi, kullanıcı talimatı harfiyen uygulattı**: Zehra `feature/zehra-slalom-core`'a yeni bir commit attı (`83d9f6a`, "gövde mekaniğin son tasarımına [`tulpar_govde_final.stl`] geçirildi") ve Talha'ya `urdf/robot.urdf.xacro` + bu mesh'i **aynen** almasını söyledi. MD5 karşılaştırmasıyla `tulpar_govde_final.stl`'in de **bizim `tulpar_ika_assembly.stl` ile bayt-bayt özdeş** olduğu doğrulandı — yani aynı orijinal dosya üçüncü kez farklı isimle geldi (`!!tulpar ika assembly.stl` → `tulpar_yeni.stl` → `tulpar_govde_final.stl`), gerçekten farklı bir mesh değil. Ayrıca commit'i tekerlek yarıçapı/kütle (0.166/64kg→0.20/65kg) ve kamera çözünürlüğü/update_rate (640x480→320x240) değerlerini yine eski haline döndürdü — üçüncü kez aynı regresyon deseni. Bulgular kullanıcıya bildirildi; kullanıcı yine de Zehra'nın dosyasının **tamamının** aynen alınmasını istedi (bu sefer seçici koruma yapılmadı). Sonuç: `urdf/robot.urdf.xacro` Zehra'nın `83d9f6a` içeriğiyle komple değiştirildi (tekerlek/kamera regresyonu dahil), yeni `meshes/tulpar_govde_final.stl` eklendi (eski `tulpar_ika_assembly*.stl` dosyaları dokunulmadan repoda kaldı, artık kullanılmıyorlar). Gazebo'da spawn testiyle doğrulandı (hata yok). Commit `d2a97ea`, push edildi. **Not**: çifte tekerlek + test çubuğu sorunları bu commit'le geri geldi (temizlenmemiş mesh kullanılıyor) — bilerek, kullanıcı talimatıyla.
- Gün 6 (devam) **Kullanılmayan eski gövde mesh dosyaları temizlendi**: `tulpar_ika_assembly.stl` + `tulpar_ika_assembly_tekersiz.stl` artık hiçbir yerden referans edilmiyordu (gövde referansı `tulpar_govde_final.stl`'e geçti), silindi. Commit `71bfa88`.
- Gün 6 (devam) **Nav2 kurulumu (MPPI+NavFn)**: "TEB parametreleri" görevi — `teb_local_planner` Jazzy'de apt'ta yok, bunun yerine `nav2_mppi_controller` (DiffDrive motion model, min-turning-radius kısıtı yok, zero-turn doğal) + `nav2_navfn_planner` kullanıldı. Yeni: `config/nav2_params.yaml`, `launch/nav2_bringup.launch.py` (gazebo/rtabmap'ten ayrı, 3. terminal). Süreçte iki gerçek altyapı bug'ı bulundu: (1) `worlds/sim_world.sdf` + `gazebo.launch.py`'de `render_engine ogre` → `ogre2` — gpu_lidar Ogre1 ile çalışmıyordu (360 ışının tamamı sabit `range_min=0.05m` dönüyordu, hiç hata vermeden; RTAB-Map bu yüzden hiçbir zaman gerçek `/map` üretemiyordu); (2) lidar/d435if/rear_camera'nın LaserScan/Image mesajlarındaki `frame_id`, Gazebo'nun iç scoped-entity ismini kullanıyordu (`tulpar/base_footprint/ydlidar_tg30` vb.), URDF'nin gerçek link ismiyle eşleşmiyordu, RTAB-Map "frame does not exist" veriyordu — statik (sıfır offsetli) TF alias'larıyla düzeltildi. Sensör/tekerlek konum denetimi de yapıldı (trimesh ile govde mesh'e göre): iz genişliği 1.16m→1.056m, lidar taretin içindeki gerçek disk konumuna taşındı (kullanıcı onayladı, ama bkz. aşağıdaki not — bu konum sonradan yanlış çıktı), rear_camera x konumu netleşmedi (açık kaldı), d435if/rpi_hq_camera konumları görsel teyit bekliyor, yeni `gnss_antenna_link`+`aim_laser_link` eklendi. Nav2 doğrulama testleri sistem bellek sıkışması (swap doluydu) yüzünden bu oturumda yapılamadı, "reboot sonrası devam edilecek" notuyla bırakıldı. Commit `c12ceb4`.
- Gün 6 (devam, yeni oturum — 18 Temmuz 2026, Nav2 doğrulama) **`c12ceb4`'ün bıraktığı yerden devam edildi, tam Nav2 doğrulaması yapıldı, 3 gerçek bug bulunup 2'si düzeltildi**:
  - Sistem reboot sonrası temizdi (swap boş) — ogre2/TF alias düzeltmeleri canlı testle doğrulandı: `/scan` artık gerçek değişken mesafeler dönüyor (0.05m'de sabit kalmıyor), TF ağacı `tulpar/base_footprint/ydlidar_tg30`→`lidar_link` alias'ıyla sağlam.
  - **KRİTİK (yeni) — RTAB-Map soğuk-başlangıç kilitlenmesi**: `rtabmap.launch.py` ilk açılışta, TF buffer'ı henüz ısınmadan gelen ilk scan'i dönüştürmeye çalışırken "Lookup would require extrapolation into the past" hatası alırsa ("Could not convert laser scan msg!"), süreç **canlı kalıyor** (ros2 param/node-info yanıt veriyor) ama iç işleme döngüsü **sessizce ve kalıcı olarak duruyor** — `/map`, `/mapData`, `/info` bir daha asla yayınlanmıyor, hiçbir ek hata da loglanmıyor. Tek çözüm: rtabmap node'unu (sadece onu, Gazebo'ya dokunmadan) `kill -9` edip yeniden başlatmak — Gazebo/TF zaten ısınmış olduğu için ikinci deneme sorunsuz çalışıyor. [[feedback_rtabmap_slam_setup]]'a eklenecek 10. gotcha.
  - **KRİTİK (yeni) — lidar kendi gövdesini görüyordu**: `c12ceb4`'te taşınan `lidar_joint` konumu (kod yorumunda zaten "henüz görsel teyit edilmedi" diye işaretliydi) trimesh ile ölçüldüğünde gövde mesh yüzeyine sadece **16.7mm** mesafede/gömülü çıktı. Canlı `/scan` bunu doğruladı: okumaların çoğu 0.09–0.45m gibi gerçek dışı kısa mesafelerdeydi (lidar kendi gövdesini "engel" olarak görüyordu) — bu yüzden `Spin` davranışı 0.1sn'de "Collision Ahead" ile iptal oluyordu ve RTAB-Map haritası sahte yakın-engel verisiyle kirleniyordu. **Geçici düzeltme** (kullanıcı onayladı, kesin/onaylı konum DEĞİL): yerel kolon taraması (40mm yarıçap) yanıltıcı çıktığı için mesh'in TÜM gövde için global Z maksimumu (858.5mm ham koordinat) esas alındı, lidar z'si 376mm→**1050mm**'e çekildi (858.5'in ~82mm üstü, garantili temiz) — canlı testte self-hit sıfıra indi. Gerçek lidar montaj noktası mekanik ekiple/görsel teyitle netleşince değişecek.
  - **Bellek sıkışması tekrarlandı, headless modla çözüldü**: Gazebo+RTAB-Map+Nav2 aynı anda çalışırken bellek yine sıkıştı (swap'a düştü, `gz sim gui` tek başına ~3.2GB RSS), RTAB-Map'in TF senkronunu bozup `/map`'i durdurdu. `gazebo.launch.py`'ye `headless` launch argument'i eklendi (`headless:=true` → `gz sim -s`, GUI açılmaz) — bellek kullanımı 14Gi→8Gi'ye düştü, sorun çözüldü. Görsel doğrulama gereken oturumlarda `headless:=false` (varsayılan) kullanılmalı.
  - Düzeltmeler sonrası **tam Nav2 doğrulaması başarılı**: lifecycle (5 node ACTIVE), Spin (360° zero-turn, `error_code:0`), NavigateToPose (map frame'de (2,0) hedefine 0 recovery ile ulaşıldı, `error_code:0`), obstacle-avoidance kritiği (`mppi::critics::ObstaclesCritic`) yüklü ve costmap obstacle_layer scan'e bağlı.
  - **KRİTİK (yeni) — `tulpar_bt` sonsuz senkron busy-loop**: `ana_tree.xml`'in kökündeki `Repeat(num_cycles=-1)`+`ForceSuccess` deseni (Gün 3'te "kasıtlı düzeltme" diye belgelenmişti), gerçek Nav2 stack'iyle canlı test edilince BT.CPP'nin bilinen bir tuzağına çarptı: `Repeat`, child SUCCESS döndüğünde onu **aynı `tickOnce()` çağrısı içinde senkron olarak** tekrar tikliyor; `ForceSuccess` her tick'te `ReactiveFallback`'in (henüz hedef yokken normal olan) FAILURE'ını SUCCESS'e çevirdiği için sonsuz senkron döngü oluşuyordu — `main.cpp`'deki 100ms'lik throttle hiç devreye giremiyordu, 1 CPU çekirdeği ~%90 kilitleniyordu, 10 saniyede ~1.2 milyon log satırı (~100MB) üretiliyordu. **Düzeltildi**: `ana_tree.xml`'den `Repeat`/`ForceSuccess` kaldırıldı (kök artık doğrudan `ReactiveFallback`), `bt_executor_main.cpp`'nin dış döngüsü `while (rclcpp::ok() && status==RUNNING)` yerine `while (rclcpp::ok())` yapıldı ve `tree.sleep()` her tick sonrası (sonuçtan bağımsız) çağrılıyor — ağaç bittiğinde (SUCCESS/FAILURE) bir sonraki dış-döngü adımında baştan yeniden tiklenip standart BT.CPP davranışıyla otomatik yeniden başlıyor, senkron döngü riski olmadan. Canlı testte doğrulandı: CPU %86.7→%2, WaitForSystemReady + `OtonomSurus` fallback döngüsü artık ~100-300ms aralıklarla (throttle'a uygun) tekrarlıyor. Ardından pozitif senaryo da doğrulandı: `/tulpar_bt/hedef_pose`'a gerçek bir hedef yayınlanınca `OtonomSurus` gerçek bir `NavigateToPose` goal'u gönderdi, robot Gazebo'da hedefe doğru hareket etti (uçtan uca BT→Nav2→Gazebo zinciri çalışıyor).
